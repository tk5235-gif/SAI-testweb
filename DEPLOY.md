# エックスサーバー 公開手順（既存WordPressサイトの入れ替え）

## 前提：調査でわかっている現状

2026-08-10 時点で外部から確認した事実です。

| 項目 | 状態 |
|---|---|
| `saigroupe.com` | **稼働中の本番サイト**（WordPress 7.0.3） |
| ネームサーバー | `ns1〜ns5.xserver.jp` = **すでにエックスサーバー管理下** |
| サーバー | `sv16335.xserver.jp` |
| SSL | **設定済み**（https でアクセス可能） |
| メール | MXもエックスサーバー。SPFレコード設定済み |
| 既存ページ | 9ページ（`/` `/thanks/` `/contact/` `/privacy-policy/` `/recruit/` `/service/` `/vision/` `/member/` `/company/`） |

### ここから言えること

- **ドメインを新規追加する必要はありません。** すでにサーバーパネルに登録済みです。
- **ネームサーバーの変更も不要です。** DNSの待ち時間はありません。
- **SSLの設定も不要です。** すでに有効です。
- OGPタグに埋め込んだ `https://saigroupe.com/` は**正しいドメイン**でした。修正不要です。

> ⚠️ **「ドメイン設定」で `saigroupe.com` を新規追加しようとしないでください。**
> すでに登録済みなのでエラーになります。また、**「ドメイン削除」は絶対に押さないでください。**
> Webサイトのファイルとメールアカウントが消えます。

---

## ⚠️ 最初に理解しておくべき3つのリスク

これは新規公開ではなく、**稼働中の会社サイトの入れ替え**です。以下は必ず把握してください。

### リスク1：`.htaccess` が上書きされる

`dist` に入っている `.htaccess` を `public_html` に置くと、**WordPressの `.htaccess` を上書きします**。このファイルにはWordPressのパーマリンク設定が入っており、失うと元に戻すのが面倒になります。

→ **入れ替え前に、既存の `.htaccess` を必ずダウンロードして保管してください。**

### リスク2：WordPressのファイルと混在すると壊れる

WordPressのファイルを残したまま新サイトのファイルを置くと、`index.php` と `index.html` が同居して挙動が読めなくなります。

→ **混ぜないでください。** WordPressを退避してから新サイトを置きます。

### リスク3：既存ページのURLが変わる

`/contact/` → `/contact.html` のようにURLが変わります。検索結果・名刺・求人媒体に載っている旧URLが404になります。

→ **対策済みです。** `dist/.htaccess` に9ページ分の301リダイレクトを組み込んであります。

---

## 進め方：2段階で行います

**いきなり本番を入れ替えません。** まずサブドメインで公開して完全に動作確認し、問題がないと確認できてから切り替えます。

```
フェーズ1  stg.saigroupe.com で公開 → 本番には一切影響なし → 徹底的に確認
                    ↓
フェーズ2  バックアップ → 本番を入れ替え → 確認 → 問題あれば即ロールバック
```

ビルド用のバッチファイルも2つに分かれています。**間違えないでください。**

| ファイル | 用途 | robots.txt |
|---|---|---|
| `build-staging.bat` | 検証用（`stg.saigroupe.com`） | **入る**（検索避け） |
| `build.bat` | 本番用（`saigroupe.com`） | 入らない |

---

# フェーズ1：サブドメインで検証公開（本番に影響なし）

## 1-1. サブドメインを追加する

1. **サーバーパネル** にログイン
2. 「ドメイン」→ **サブドメイン設定**
3. `saigroupe.com` を選択 →「サブドメイン設定追加」タブ
4. サブドメイン名に `stg` と入力（`stg.saigroupe.com` になります）
5. 「無料独自SSLを利用する」に **チェックを入れて** 追加

SSL発行に最大1時間かかります。

作成されるフォルダはここです。

```
/home/（サーバーID）/saigroupe.com/public_html/stg/
```

> **注意：** このフォルダはWordPressのドキュメントルートの中に作られます。
> WordPress管理画面で「stg」というスラッグの固定ページを作らないでください。競合します。

## 1-2. 検証用ビルドを作る

**`build-staging.bat` をダブルクリック**してください。`build.bat` ではありません。

```bash
node build-dist.js --staging
```

検証用サイトが検索結果に出ると本番と重複コンテンツになるため、このビルドには検索避けの `robots.txt`（`Disallow: /`）が自動で入ります。コンソールに `[検証用ビルド]` と表示されることを確認してください。

> フェーズ2で本番へ上げるときは、**必ず `build.bat` で作り直します。**
> 検証用ビルドをそのまま本番に置くと、サイトが検索結果から消えます。
> `dist` は毎回まるごと作り直されるので、手作業での消し忘れは起きません。

## 1-3. アップロード

`dist` の**中身**を上記の `stg/` フォルダへアップロード。

詳細な操作は「[FTPでのアップロード方法](#ftpでのアップロード方法)」を参照してください。

## 1-4. 徹底的に確認する

`https://stg.saigroupe.com/` を開いて、以下をすべて確認します。

**表示の確認**
- [ ] トップページが表示される
- [ ] 全10ページを開いて崩れ・画像切れがない
- [ ] スマホ実機で全ページを確認

**フォームの確認（ここが最重要）**
- [ ] お問い合わせフォームを送信 → `thanks.html` に遷移する
- [ ] `info@saigroupe.com` に内容が届く
- [ ] 送信者アドレスに自動返信が届く
- [ ] メールが文字化けしていない
- [ ] 届いたメールに「返信」すると、宛先が送信者本人になる
- [ ] エントリーフォームも同様に確認（`recruit/thanks.html` に遷移）
- [ ] 迷惑メールフォルダに入っていないか確認

**ここで問題が出ても本番は無傷です。** 遠慮なく試してください。エラーが出たら画面の文言かエラーログを教えてください。

---

# フェーズ2：本番の入れ替え

フェーズ1がすべてクリアできてから進めます。**アクセスの少ない時間帯**（早朝・深夜）を推奨します。

## 2-1. バックアップを取る ★必須

### ① ファイル一式（FTP）

FTPで `public_html` の中身を**まるごとPCにダウンロード**します。時間はかかりますが、これが最も確実な保険です。

特に以下は必ず含まれていることを確認してください。

- `.htaccess` ← 隠しファイル。表示設定をONにしないと見えません
- `wp-config.php` ← データベース接続情報
- `wp-content/` ← テーマ・プラグイン・アップロード画像すべて

### ② データベース

1. サーバーパネル →「データベース」→ **phpMyAdmin**
2. `wp-config.php` に書かれているユーザー名／パスワードでログイン
3. 左のデータベース名をクリック →「エクスポート」タブ →「実行」

### ③ 保存場所を控える

ダウンロードしたフォルダは、日付をつけて分かる場所に置いてください。

```
例）C:\Users\takoy\Desktop\saigroupe_backup_20260810\
```

> エックスサーバーには自動バックアップ機能もありますが、**自分で取ったバックアップが最優先**です。
> 自動バックアップは保持期間に上限があり、いざというとき確実とは限りません。

## 2-2. WordPressを退避する

**削除ではなく、移動します。** すぐ戻せる状態にしておくためです。

FTPで `public_html` の中に `_wp_old` というフォルダを作り、既存のファイルをすべてその中に移動します。

```
public_html/
  ├─ _wp_old/          ← 既存のWordPress一式をここへ移動
  │    ├─ .htaccess
  │    ├─ index.php
  │    ├─ wp-config.php
  │    ├─ wp-content/
  │    └─ …
  └─ （ここに新サイトを置く）
```

> FTPソフト上でドラッグして移動できます。ファイル数が多いので時間がかかります。
> **データベースは消さないでください。** 戻すときに必要です。

## 2-3. 新サイトをアップロード

1. **`build.bat`**（本番用）をダブルクリック。コンソールに `[本番用ビルド]` と出ることを確認
   - `build-staging.bat` ではありません。検索避けの `robots.txt` が入ってしまいます
2. `dist` に `robots.txt` が**無い**ことを目視で確認
3. `dist` の**中身**を `public_html/` 直下にアップロード
4. `.htaccess` がアップロードされているか確認（隠しファイル表示をONに）

## 2-4. 動作確認

- [ ] `https://saigroupe.com/` が新サイトになっている
- [ ] `https://saigroupe.com/recruit/` が採用サイトになっている
- [ ] 旧URLが転送される
  - `https://saigroupe.com/contact/` → `/contact.html`
  - `https://saigroupe.com/company/` → `/#company`
  - `https://saigroupe.com/service/` → `/#service`
- [ ] フォームを**本番でもう一度**送信テスト
- [ ] `http://saigroupe.com/` が https に転送される
- [ ] スマホ実機で確認

## 2-5. 公開後の仕上げ

- [ ] Google Search Console に新しいサイトマップを登録
- [ ] SNSにURLを貼り、サムネイル画像が出るか確認
- [ ] `stg.saigroupe.com` のサブドメインを削除（不要になったら）

---

## 🔙 ロールバック手順（元に戻したいとき）

慌てず、この順でやれば戻せます。

1. FTPで `public_html` 直下の新サイトのファイルをすべて削除
2. `_wp_old/` の中身を `public_html/` 直下に戻す
3. `.htaccess` が戻っているか確認
4. `https://saigroupe.com/` を開いて旧サイトが表示されることを確認

**データベースを消していなければ、これだけで完全に元通りになります。**
だからこそ 2-2 で「削除ではなく移動」にしています。

---

## 方法A：ファイルマネージャ（ソフト不要・推奨）★

ブラウザだけで完結します。FTPソフトのインストールが不要なので、こちらが確実です。

`build.bat` / `build-staging.bat` を実行すると、`dist` フォルダと一緒に **`dist.zip`** が自動生成されます。これをアップロードして解凍するだけです。

> **Windowsの右クリック「ZIPファイルに圧縮する」は使わないでください。**
> `.htaccess` が含まれないことがあります。ビルドが作る `dist.zip` は、
> `.htaccess` を含み、かつフォルダ区切りを `/` で書き込んだLinux互換のZIPです。

### ファイルマネージャの場所

**サーバーパネルの中にはありません。** 独立したツールで、専用のログインページから入ります。

```
https://secure.xserver.ne.jp/xapanel/login/xserver/ftp/
```

> Xserverアカウントにログインし、「ご契約一覧」→ サーバー →「**ファイル管理**」からも開けます。
> サーバーパネルの「ホームページ」メニューを探しても見つかりません。

**ログインに使うのはサーバーパネルのIDではなく、FTPアカウントの情報です。**

| 項目 | 値 |
|---|---|
| FTPユーザーID | サーバーID（`xs` で始まる文字列） |
| FTPパスワード | サーバーパスワード |

分からない場合は サーバーパネル →「FTP」→ **FTPアカウント設定** →「FTPソフト設定」タブで確認できます。

### 手順

1. 上記URLからファイルマネージャにログイン
2. `saigroupe.com` → `public_html` → `stg` の順にフォルダを開く
   - 本番へ上げるときは `public_html` 直下
3. 画面上部の「**アップロード**」をクリックし、表示された領域に `dist.zip` を**ドラッグ&ドロップ** →「アップロード」
4. アップロードされた `dist.zip` を選択 → 「**展開**」をクリック
   - ボタン名は「解凍」ではなく「**展開**」です
5. 展開が終わったら、`dist.zip` 本体を選択して「**削除**」
   - 消さないと `https://stg.saigroupe.com/dist.zip` で誰でもダウンロードできてしまいます

### 展開後に必ず確認すること

ファイルマネージャの一覧に以下があるか見てください。

- [ ] `.htaccess` がある（ファイルマネージャは隠しファイルも表示します）
- [ ] `index.html` `send.php` `robots.txt` がある
- [ ] `images` `recruit` が**フォルダとして**存在する
  - もし `images/ceo.jpg` のような名前の**ファイル**が並んでいたら、ZIPの作り方が間違っています。ビルドで生成した `dist.zip` を使い直してください

### 更新するとき

同じ手順で上書きします。展開時に「上書きしますか」と聞かれたらOKしてください。
古いファイルが残るのが気になる場合は、先に中身を全削除してから展開します。

---

## 方法B：FTPソフト（FileZilla）

> **FileZillaのインストーラは「望ましくないアプリ」としてブラウザやWindows Defenderにブロックされることがあります。**
> 提携ソフトを同梱しているためで、無理に許可する必要はありません。
> その場合は**方法A**を使うか、同梱物のない [WinSCP](https://winscp.net/eng/docs/lang:jp)（日本語対応・無料）を使ってください。
> WinSCPでも接続情報は下記と同じです。

推奨ソフトは [FileZilla](https://filezilla-project.org/ja/)（無料）です。
**ダウンロードページでは「FileZilla」を選んでください。** 4種類並んでいますが、`Bundle` は提携ソフト同梱、`Pro` は有料版で、いずれも今回は不要です。

> インストーラで提携ソフトの同梱を勧められたら「Decline」「同意しない」を選んで進めてください。

### STEP A. 接続情報を調べる

サーバーパネル →「FTP」→ **FTPアカウント設定** → 対象ドメインを選択 →「FTPソフト設定」タブ。

| 項目 | 値 |
|---|---|
| FTPホスト（サーバー名） | `sv16335.xserver.jp` |
| ユーザー名（アカウント名） | サーバーID（`xs` で始まる文字列） |
| パスワード | サーバーパスワード |
| ポート | `21` |

サーバーIDが分からない場合は、Xserverアカウントの「契約管理」画面か、契約時のメール（件名に「サーバーアカウント設定完了のお知らせ」）に載っています。

### STEP B. 隠しファイルを表示する設定 ★最重要

`.htaccess` は**ドットで始まる隠しファイル**で、初期設定では画面に出ません。見えないままだと転送されず、リダイレクトが効きません。

**メニュー「サーバー」→「強制的に隠しファイルを表示する」にチェック**を入れてください。

> この設定は接続前でも後でも構いませんが、**転送より前に**必ず入れてください。
> フェーズ2でバックアップを取るときも、これが無いと `.htaccess` を取り逃します。

### STEP C. サイトマネージャーで接続先を登録する

毎回入力せずに済むので、初回だけ登録しておくのがおすすめです。

1. メニュー「ファイル」→ **サイトマネージャー**
2. 「新しいサイト」をクリックし、名前を `SAI Xserver` などに
3. 右側を以下のように設定

| 項目 | 設定値 |
|---|---|
| プロトコル | FTP - ファイル転送プロトコル |
| ホスト | `sv16335.xserver.jp` |
| ポート | `21` |
| 暗号化 | **明示的な FTP over TLS が必要** |
| ログオンタイプ | 通常 |
| ユーザー | サーバーID |
| パスワード | サーバーパスワード |

4. 「接続」をクリック
5. 証明書の確認ダイアログが出たら「**今後もこの証明書を常に信用する**」にチェックして OK

接続すると画面下のログに `ディレクトリリストの表示成功` と出ます。

> **接続できないとき**
> - `530 Login authentication failed` → ユーザー名かパスワードの誤り。FTPアカウントのパスワードとサーバーパスワードは別物なので注意
> - タイムアウトする → 会社ネットワークが21番ポートを塞いでいる可能性。テザリング等で試す
> - エックスサーバーには**海外アクセス制限**があります。VPN経由だと弾かれることがあるのでOFFに

### STEP D. 画面の見方

FileZillaは左右に分かれています。

```
┌─────────────────┬─────────────────┐
│  左 = 自分のPC   │  右 = サーバー   │
│  （ローカル）    │  （リモート）    │
└─────────────────┴─────────────────┘
```

**左（PC側）**：`C:\Users\takoy\Projects\Claud\sai-redesign\dist` を開く
**右（サーバー側）**：下記のパスまで移動

検証用（フェーズ1）
```
/home/（サーバーID）/saigroupe.com/public_html/stg/
```

本番（フェーズ2）
```
/home/（サーバーID）/saigroupe.com/public_html/
```

> 右側上部の「リモートサイト」欄にパスを直接貼り付けて Enter でも移動できます。
> `home` → サーバーID → `saigroupe.com` → `public_html` → `stg` の順にダブルクリックでも辿れます。

### STEP E. 転送する

1. **左側**（PC側）のファイル一覧をクリックしてから **Ctrl + A** で全選択
   - `dist` フォルダ**ごと**ではなく、**中身**を選ぶのがポイント
   - フォルダごと入れると `stg/dist/` になり、URLが `https://stg.saigroupe.com/dist/` になってしまいます
2. 選択したまま**右側へドラッグ&ドロップ**（または右クリック →「アップロード」）
3. 画面下の「転送キュー」が空になるまで待つ（約1.9MB / 48ファイル、数十秒程度）

**転送後に必ず確認すること**

- [ ] 画面下のタブ「**失敗した転送**」に何も無い（あれば右クリック →「失敗した転送を再処理」）
- [ ] 右側に `.htaccess` が見えている ← STEP B をやっていないと見えません
- [ ] 右側に `index.html` `send.php` `robots.txt` `images` `recruit` がある
- [ ] `recruit` フォルダを開くと `index.html` や `recruit.css` が入っている

### STEP F. ブラウザで開く

`https://stg.saigroupe.com/`

「無効なURLです」からサイト表示に変われば成功です。表示が変わらない場合は `Ctrl + Shift + R` で強制再読み込みしてください。


---

## 2回目以降の更新手順

入れ替えが済んだあとの通常運用は、この2ステップだけです。

1. `build.bat` をダブルクリック
2. `dist` の中身を `public_html/` にアップロード（上書き）

```bash
node build-dist.js
```

> **`git push` してもエックスサーバーには反映されません。**
> `https://tk5235-gif.github.io/SAI-testweb/` は検証用として残しています。

---

## 設定を変えたいとき

### 送信先アドレスを変える

`send.php` の冒頭を編集して、再ビルド→再アップロード。

```php
$TO_ADDRESS   = 'info@saigroupe.com';   // 受信先（カンマ区切りで複数可）
$FROM_ADDRESS = 'info@saigroupe.com';   // 差出人（必ず saigroupe.com のアドレス）
$AUTO_REPLY   = true;                   // 自動返信を止めるなら false
```

`$FROM_ADDRESS` を他社ドメイン（Gmail等）にすると、SPF判定に失敗して**ほぼ確実に迷惑メール扱い**になります。`saigroupe.com` のアドレスのままにしてください。

### 受信を普段のアドレスにしたい

`send.php` は触らず、サーバーパネル →「メール」→ **メールアカウント設定** → 転送設定で転送先を追加するのが安全です。

### PHPのバージョン確認

サーバーパネル →「PHP」→ **PHP Ver.切替** で **PHP 8.0以上**を確認してください。

---

## トラブルシューティング

| 症状 | 原因と対処 |
|---|---|
| 旧WordPressサイトのまま表示される | `index.php` が残っている。`_wp_old` への移動漏れを確認 |
| デザインが崩れる | `style.css` / `recruit/recruit.css` のアップロード漏れ。フォルダ構成を維持して再アップ |
| 画像だけ出ない | `images` フォルダの漏れ。**大文字小文字も区別される**ので注意 |
| 旧URLが転送されない | `.htaccess` のアップロード漏れ。隠しファイル表示をONにして確認 |
| 送信すると500エラー | `send.php` のPHPエラー。サーバーパネル →「PHP」→ **エラーログ** で原因を確認 |
| `send.php` の中身がテキストで表示される | PHPが動いていない。PHPバージョン設定を確認 |
| 「送信できませんでした」の赤い画面 | 画面の文言を控えてください。未入力かメール送信失敗かが文言で判別できます |
| メールが届かない | ①迷惑メールフォルダ ②`info@saigroupe.com` が存在するか ③`$FROM_ADDRESS` が saigroupe.com か |
| サイトが検索結果から消えた | 本番に検証用ビルドを上げてしまった。`build.bat`（本番用）で作り直して再アップ |
| 更新したのに古い表示 | ブラウザキャッシュ。`Ctrl + Shift + R` |

---

## まだ解決していない課題

公開判断の前に確認が必要なものです。

- **インタビュー本文が実取材ではありません。** 実在の社員名（藤平勇太・伊藤義将・根岸悠河・藤平健太）にこちらで作成した発言が紐づいた状態です。社外公開前にご本人確認をお願いします。
- **採用トップ「人を知る」が3名のまま**で、インタビューページの4名と一致していません。
