# エックスサーバー 公開手順

株式会社SAI コーポレートサイト＋採用サイトを、エックスサーバーの独自ドメインで公開する手順。
初回は STEP 0〜7 を順に、2回目以降の更新は **STEP 5 だけ** でOK。

> 管理画面のメニュー名はエックスサーバー側の改修で変わることがあります。
> 見当たらない場合は同じ意味の項目を探してください。

---

## STEP 0. 事前に手元に用意するもの

| 必要なもの | 入手先 |
|---|---|
| Xserverアカウントのログイン情報 | 契約時のメール |
| サーバーパネルのログイン情報 | 契約時のメール（アカウント画面からも入れる） |
| 公開したいドメイン名 | 取得済みのもの |
| FTPソフト | [FileZilla](https://filezilla-project.org/)（無料）。使わずWebのファイルマネージャでも可 |

**サーバーID**（例 `xs123456`）は後で使うので控えておいてください。

---

## STEP 1. ドメインをサーバーに紐づける

### 1-A. ドメインをエックスサーバー以外で取得した場合（お名前.com等）

先にネームサーバーを変更します。**反映に数時間〜最大72時間**かかるので、ここから着手してください。

取得元の管理画面で、ネームサーバーを以下に変更します。

```
ns1.xserver.jp
ns2.xserver.jp
ns3.xserver.jp
ns4.xserver.jp
ns5.xserver.jp
```

### 1-B. サーバーパネルにドメインを追加

1. **サーバーパネル** にログイン
2. 「ドメイン」→ **ドメイン設定**
3. 「ドメイン設定追加」タブ → ドメイン名を入力
4. 以下2つに **チェックを入れたまま** 追加する
   - ☑ 無料独自SSLを利用する
   - ☑ 高速化・アクセス数拡張機能「Xアクセラレータ」を有効にする

SSLの発行には**最大1時間程度**かかります。この間に STEP 2〜4 を進めてください。

> ネームサーバーの切替が終わっていないとSSLの発行に失敗します。
> 失敗した場合は STEP 4 で手動で追加できるので、慌てなくて大丈夫です。

---

## STEP 2. PHPのバージョンを確認する

フォーム（`send.php`）が動く環境かの確認です。

1. サーバーパネル →「PHP」→ **PHP Ver.切替**
2. 対象ドメインを選択
3. **PHP 8.0 以上**（推奨は最新の安定版）になっていることを確認。低ければ切り替える

---

## STEP 3. 送信元メールアドレスを作る ★重要

フォームの差出人になる `info@（あなたのドメイン）` を、**このサーバー上に実在させます**。

1. サーバーパネル →「メール」→ **メールアカウント設定**
2. 対象ドメインを選択 →「メールアカウント追加」
3. メールアカウント名に `info` を入力、パスワードを設定して追加

### なぜ必要か

エックスサーバーは自社ドメインの送信に自動でSPFレコードを設定します。差出人を**そのサーバーのドメイン**にすることで「なりすましではない」と判定され、迷惑メールフォルダに落ちにくくなります。

逆に、差出人をGmailなど他社ドメインにすると**ほぼ確実に迷惑メール扱い**になります。`send.php` の `$FROM_ADDRESS` は必ず独自ドメインのアドレスにしてください。

### 受信先を変えたい場合

届いたメールを普段使いのアドレスで受け取りたいときは、同じ画面の「転送設定」で転送先を追加してください。`send.php` 側は触らずに済みます。

---

## STEP 4. SSL（https化）の状態を確認する

1. サーバーパネル →「ドメイン」→ **SSL設定** → 対象ドメインを選択
2. 「SSL設定一覧」に対象ドメインが載っていればOK
3. 載っていなければ「独自SSL設定追加」タブから追加

`https://（あなたのドメイン）` を開いて鍵マークが出れば完了です。まだ「保護されていない通信」と出る場合は、反映待ちなので1時間ほど置いてから再確認してください。

---

## STEP 5. サイトをアップロードする ← **更新時はここだけ**

### 5-1. アップロード用フォルダを作る

プロジェクトフォルダの **`build.bat` をダブルクリック** します。

```bash
node build-dist.js
```

`dist` フォルダが自動で開きます。ここには公開して良いファイルだけが入っています。

- 社内資料（`HANDOVER.md` / `SHOOT-LIST.md`）は**除外済み**
- どこからも参照されていない画像 約24MB も**除外済み**
- 常時SSL化する `.htaccess` を**同梱済み**
- 合計 約1.9MB / 48ファイル

> `dist` は毎回まるごと作り直されます。`dist` の中を直接編集しても次回消えるので、修正は必ず元のファイル側で行ってください。

### 5-2. アップロード先

```
/home/（サーバーID）/（ドメイン名）/public_html/
```

例）サーバーIDが `xs123456`、ドメインが `saigroupe.com` の場合
`/home/xs123456/saigroupe.com/public_html/`

### 5-3. アップロード（FileZillaの場合）

FTP情報はサーバーパネル →「FTP」→ **FTPアカウント設定** で確認できます。

| 項目 | 値 |
|---|---|
| ホスト | `sv〇〇〇〇.xserver.jp`（FTPアカウント設定画面に表示） |
| ユーザー名 | サーバーID |
| パスワード | サーバーパスワード |
| ポート | 21 |
| 暗号化 | 「明示的なFTP over TLS」を推奨 |

1. FileZillaで接続し、右側（サーバー側）を `public_html` まで移動
2. **`public_html` に最初から入っているファイルを削除する**
   - `index.html`（エックスサーバーの初期ページ）
   - `default_page.png`
   - これらを消さないと、初期ページのままサイトが表示されません
3. 左側（自分のPC）で `dist` フォルダを開く
4. **`dist` フォルダごとではなく、`dist` の"中身"を全選択**して右側にドラッグ

> `dist` フォルダごと入れると `https://ドメイン/dist/` になってしまいます。中身だけを入れてください。

### 5-4. 隠しファイルに注意

`.htaccess` は**ドット始まりの隠しファイル**です。FileZillaのメニュー「サーバー」→「強制的に隠しファイルを表示」にチェックを入れて、アップロード漏れがないか確認してください。

### ファイルマネージャ（Web）を使う場合

FTPソフトを入れたくない場合は、サーバーパネル →「ホームページ」→ **ファイルマネージャ** からブラウザ上でアップロードできます。ただしファイル数が多いと時間がかかるので、`dist` をZIPにまとめてアップロード→サーバー上で解凍するのが早いです。

---

## STEP 6. フォームの送信テスト ★必須

**`send.php` は実際のサーバーでしか動作確認できません。必ずテストしてください。**

### 6-1. お問い合わせフォーム

1. `https://（ドメイン）/contact.html` を開く
2. 全項目を入力して送信
3. 確認すること
   - [ ] `https://（ドメイン）/thanks.html` に遷移する
   - [ ] `info@（ドメイン）` に問い合わせ内容のメールが届く
   - [ ] 入力したアドレスに自動返信が届く
   - [ ] 届いたメールが**文字化けしていない**
   - [ ] メールに「返信」すると、送信者本人のアドレスが宛先に入る

### 6-2. エントリーフォーム

`https://（ドメイン）/recruit/entry.html` で同様にテストし、`recruit/thanks.html` に遷移することを確認します。

### 6-3. 迷惑メールフォルダも必ず確認

受信トレイに無くても、迷惑メールフォルダに入っていることがあります。入っていた場合は「迷惑メールではない」を選択してください。

---

## STEP 7. 公開後の仕上げ

- [ ] スマホ実機で全ページを確認
- [ ] `https://（ドメイン）` と `http://（ドメイン）` の両方を開き、httpが自動でhttpsに転送されるか確認
- [ ] SNS（X・Facebook・LINE）にURLを貼り、サムネイル画像が出るか確認
- [ ] Google Search Console にサイトを登録
- [ ] 名刺・求人媒体などに載せる旧URLがあれば差し替え

---

## 設定を変えたいとき

### 送信先アドレスを変える

`send.php` の冒頭を編集し、`build.bat` → 再アップロード。

```php
$TO_ADDRESS   = 'info@saigroupe.com';   // 受信先（カンマ区切りで複数可）
$FROM_ADDRESS = 'info@saigroupe.com';   // 差出人（必ず独自ドメインのアドレス）
$AUTO_REPLY   = true;                   // 自動返信を止めるなら false
```

### 自動返信の文面を変える

`send.php` 内の `$replyLead` と、自動返信の署名部分を編集します。

### ドメインが `saigroupe.com` ではない場合 ★確認事項

OGP・canonicalタグに `https://saigroupe.com/` が**ハードコードされています**。実際のドメインが異なる場合は全HTMLの一括置換が必要です。放置するとSNSでサムネイルが出ず、検索エンジンにも誤ったURLを伝えることになります。

---

## トラブルシューティング

| 症状 | 原因と対処 |
|---|---|
| エックスサーバーの初期ページが出る | `public_html` の初期 `index.html` が残っている。削除する |
| ページは出るがデザインが崩れる | `style.css` / `recruit/recruit.css` のアップロード漏れ。フォルダ構成を維持して再アップ |
| 画像だけ表示されない | `images` フォルダごとアップロードできているか確認。**大文字小文字も区別される**ので注意 |
| 送信すると「500 Internal Server Error」 | `send.php` のPHPエラー。サーバーパネル →「PHP」→ **エラーログ** で原因を確認 |
| 送信すると `send.php` の中身がテキストで表示される | PHPが動いていない。STEP 2 のPHPバージョン設定を確認 |
| 「送信できませんでした」と赤い画面が出る | 画面の文言をそのまま控えてください。必須項目未入力か、メール送信自体の失敗かが文言で判別できます |
| メールが届かない | ①迷惑メールフォルダを確認 ②STEP 3 のメールアカウントを作成したか ③`$FROM_ADDRESS` が独自ドメインか |
| メールが文字化けする | サーバーのPHPが古い可能性。PHP 8系に切替 |
| httpsにならない | SSL反映待ち（最大1時間）。1時間以上経つならSTEP 4でSSL設定を追加 |
| 更新したのに古い表示のまま | ブラウザキャッシュ。`Ctrl + Shift + R`。それでも古いならアップロード先のフォルダが違う可能性 |

---

## GitHub Pages との関係

`https://tk5235-gif.github.io/SAI-testweb/` は**検証用として残しています**。`git push` すると引き続きそちらが更新されますが、**本番のエックスサーバーには反映されません**。

本番を更新する手順は常にこの2つです。

1. `build.bat` を実行
2. `dist` の中身を `public_html` にアップロード
